// Generated by CoffeeScript 1.3.3
'use strict';

angular.module('Courses.services', []).service('Semester', function() {
  return this.getValidSemesters = function() {
    var effectiveMonth, i, month, semester, semesters, year, _i;
    semesters = [];
    month = new Date().getMonth() + 1;
    year = new Date().getFullYear();
    effectiveMonth = month + 1;
    for (i = _i = 0; _i <= 2; i = ++_i) {
      if (effectiveMonth > 11) {
        effectiveMonth %= 12;
        year++;
      }
      semester = Math.floor(effectiveMonth / 4) + 1;
      effectiveMonth += 4;
      semesters.push(year + '' + semester);
    }
    return semesters;
  };
});
