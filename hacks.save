for each(picture on columbialions.com)
	create(profile for person)
		Expectation = 100 / 10*36-r;
	end
end

if(click on left)
	rA = Ea/Eb;
else
	rB = Eb/Ea;
end

if(click on list)
	list by expectation;
end

if(click on byTeam)
	list by team
		list by expectation
end

if(click on byGender)
	split by gender
		list by expectation
end
